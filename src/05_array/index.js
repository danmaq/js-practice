// 配列を使ったテンプレート リテラル
let spells = ['弱', '強'];
let word = `${spells[0]}肉${spells[1]}食`;
console.log(word);


// ジャグ配列
let identity = [[1, 0, 0], [0, 1, 0], [0, 0, 1]];
console.log(identity);
let m23 = identity[1][2];
console.log(m23);
